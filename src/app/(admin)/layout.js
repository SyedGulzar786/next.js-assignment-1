import "../globals.css";
import Link from "next/link";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <Link className="underline ml-4 mr-4 hover:no-underline text-blue-500 hover:text-blue-700 font-bold" href={"/admin"}>admin</Link>
        <Link className="underline ml-4 mr-4 hover:no-underline text-blue-500 hover:text-blue-700 font-bold" href={"/admin/batches"}>batches</Link>
        <Link className="underline ml-4 mr-4 hover:no-underline text-blue-500 hover:text-blue-700 font-bold" href={"/admin/courses"}>courses</Link>
        <Link className="underline ml-4 mr-4 hover:no-underline text-blue-500 hover:text-blue-700 font-bold" href={"/admin/teachers"}>teachers</Link>
        {children}
      </body>
    </html>
  );
}